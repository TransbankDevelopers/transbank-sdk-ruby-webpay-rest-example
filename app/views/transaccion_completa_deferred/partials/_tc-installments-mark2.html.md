```ruby
    {
      "installments_amount": '<%= @resp["installments_amount"] %>',
      "id_query_installments": '<%= @resp["id_query_installments"] %>',
      "deferred_periods": '<%= @resp["deferred_periods"] %>'
    }
```